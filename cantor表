#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	int i=0;
	int j=0;
	while(n>j)
	{
		i++;
		j+=i;
	} 
	if(i%2==0)
	{
		printf("%d/%d\n",i+n-j,j-n+1);
	}
	else
	printf("%d/%d\n",j-n+1,i+n-j);
	return 0;
}
