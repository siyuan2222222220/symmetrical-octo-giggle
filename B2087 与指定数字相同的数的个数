#include<stdio.h>
int main()
{
	//B2087 与指定数字相同的数的个数
	int n;
	scanf("%d",&n);
	int arr[105]={0};
	int i=0;
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
	int m;
	scanf("%d",&m);
	int ans=0;
	for(i=0;i<n;i++)
	{
		if(arr[i]==m)
		{
			ans++;
		}
	}
	printf("%d\n",ans);
	return 0; 
}
