#include<stdio.h>
int arr[100001]={0};
int main()
{
	int n;
	scanf("%d",&n);
	int num;
	int max=0;
	int i=0;
	for(i=0;i<n;i++)
	{
		scanf("%d",&num);
		arr[num]++;
		if(num>max)
		max=num;
	}
	for(i=0;i<=max;i++)
	{
		printf("%d\n",arr[i]);
	}
	return 0;
}
