#include<stdio.h>
int main()
{
	//B2081 与 7 无关的数
	int n;
	scanf("%d",&n);
	long long sum=0;
	int i=0;
	for(i=1;i<=n;i++)
	{
			if(i%7==0||i/10==7||i%10==7)
			{
				;
			}
			else
			sum+=i*i;	
	}
	printf("%lld\n",sum);
	return 0;
}
