#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	int gold,silver,metal;
	int sum1=0,sum2=0,sum3=0;
	int i=0;
	for(i=0;i<n;i++)
	{
		scanf("%d%d%d",&gold,&silver,&metal);
		sum1+=gold;
		sum2+=silver;
		sum3+=metal;
	}
	printf("%d %d %d %d\n",sum1,sum2,sum3,sum1+sum2+sum3);
	return 0;
}
