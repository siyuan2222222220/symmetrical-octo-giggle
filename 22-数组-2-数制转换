#include<stdio.h>
#include<string.h> 
#include<math.h>
int main()
{
	int num;
	scanf("%lld",&num);
	int p;
	scanf("%d",&p);
	long long t=0; 
	int arr[len]
	while(num>0)
	{
		int tmp=num%p;
		int i=0;
		for(i=0;i<p;i++)
		{
			
		}
		num/=p;
	}
	printf("%lld\n",t);
	return 0; 	
 } 
