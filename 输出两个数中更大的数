#include<stdio.h>
//输出两个数中更大的数 
//定义函数
int get_max(int x,int y)
{
	if(x>y)
	return x;
	else
	return y;
 } 
 int main()
 {
 	int a=10;
 	int b=20;
 	//函数的使用
	 int max=get_max(a,b);
	 printf("max=%d\n",max);
	 return 0; 
 }
