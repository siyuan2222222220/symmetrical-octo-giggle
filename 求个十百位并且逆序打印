//有一个三位以下的正整数，
//求个十百位并且逆序打印
#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	int g,s,b;
	g=n%10;
	s=n/10%10;
	b=n/100;
	if(b!=0)
	{
		printf("%d %d %d\n",g,s,b);
	}
	else if(s!=0)
	{
		printf("%d %d\n",g,s);
	}
	else if(g!=0)
	{
		printf("%d\n",g);
	}
	return 0;	
} 
