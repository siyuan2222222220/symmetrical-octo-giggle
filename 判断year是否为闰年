//判断year是否为闰年
#include<stdio.h> 
int main(){
	int year=0;
	int cnt=0;
	for(year=1000;year<=2000;year++){
		
		//能被4整除并且不能被100整除
		//或者能被400整除 
		if(year%4==0&&year%100!=0||year%400==0)
	{
		printf("%d ",year);
		cnt++;
	
	}
	}
	printf("\n%d\n",cnt);
	return 0;
} 
