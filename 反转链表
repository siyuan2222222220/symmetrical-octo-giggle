#include<stdio.h>
#include<string.h>
char s[10000];
int main()
{
	while(gets(s)!=NULL)
	{
		int len=strlen(s);
		int i=0;
		for(i=len-1;i>=0;i--)
		{
			printf("%c",s[i]);	
		}
		printf("\n");		
	}
		
	return 0;
} 
