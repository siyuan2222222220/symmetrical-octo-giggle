#include<stdio.h>
#include<string.h>
int main()
{
	char ch[260];
	gets(ch);
	int len=strlen(ch);
	int i=0;
	int cnt=0;
	for(i=0;i<len;i++)
	{
		if(ch[i]>='0'&&ch[i]<='9')
		cnt++;
	}
	printf("%d\n",cnt);
	
}
