#include<stdio.h>
#include<string.h>
int main()
{
	char ch[1000];
	while(scanf("%s",ch)!=EOF)
	{
		int len=strlen(ch);
		int i=0;
		for(i=0;i<len;i++)
		{
			if(ch[i]=='T')
			ch[i]='U';
			printf("%c",ch[i]);	
		}
		printf("\n");	
	}
	return 0;
}
